2026-08-19
## block1 
### 1. 구현 목적: 
- Role/Permission system 구현 

### 2.개발흐름 :
1. role class에 디폴트 및 퍼밋값 설정/퍼밋여부를 2의제곱으로 표현 #퍼밋함수 
	-> 각 퍼밋은 서로 침범하지 않는 각 공간에 부여 
2. **`Role.__init__()`*** 을 재정의 
	-> 부모(SQLAlchemy)의 객체 초기화는 유지하면서, 생성 시 `permissions`가 `None`이면 bit 연산이 가능하도록 `0`으로 보장하기 위해.
3. 퍼밋 부여,삭제, 확인, 리셋 함수 제작 

### 3.병목점/이유: 
1. ***Role__init__()*** 재정의 없이 코드 작성/객체 다루는 시점에 None이 발생 
	- 부모 초기화 실행 
2. 인풋인 str type의 값을 메소드 이름으로 사용
	- getattr(object, value)를 사용할 것 



## block2 
### 1. 구현 목적: 
creating roles  
### 2. 개발흐름 : 
1. 롤에 퍼밋 할당 함수를 제작 
2. 함수 안에는 딕셔너리 형태로 각각의 역할과 그에 따른 퍼밋 값 작성 
3. 반복문을 돌면서 각 롤마다 퍼밋 할당 
### 3.병목점/이유: 
1. 디폴트값 설정 x
	-> 신규 사용자에게 별도 판단 없이 부여할 값이 필요 
---
배울 지점: 
- bit flag 권한 설계
- super()이용한 초기화 확장 
- default role이라는 app정책 
- staticmethod와 instance method의 책임 차이 
- `dict를 직접 for문으로 순회하면 key가 나온다`
## next: role assignment 