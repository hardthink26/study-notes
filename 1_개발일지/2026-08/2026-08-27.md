# 개발일지 

## block1 
### 1. 구현 목적: test함수 setup, teardown삭제+ db하나만 사용, 각 함수마다 초기화 

### 2.개발흐름 :
1. user_db fixture 제작(범위:클래스, 역할:DB create, drop), initialize_db(범위:함수마다, 역할: 함수 끝날때마다 DB초기화)
2. class에 fixture 넣기(@pytest.mark.usefixtures("initialize_db")) #deep-dive 

### 3.병목점/이유: 
1. app_context의 의미 파악 x
2. yield의 내부원리는 이해부족 #deep-dive 

## block2
### 1. 구현 목적: Administrator-Level Profile Editor  -> form 제작 

### 2.개발흐름 :
1. 입력form 제작 
2. 실제 유저 들어올 시 부모 초기화 함수 제작 
3. email, username수정 필드 만들기 
### 3.병목점/이유: 
1. name, location에서 length validator설정 안함 
-> 입력 제약 고려합니다. 
2. 클래스에서 부모 초기화 및, *args. **kwargs 이해 안됨 
3. **`SelectField.choices는 (value, label) 튜플의 리스트로 구성한다.`** 

## block3
### 1. 구현 목적: 

### 2.개발흐름 :

### 3.병목점/이유: 
---
### 배울 지점: 
1. form field를 제작시 데이터 제약도 같이 점검 
2. 상속받은 클래스에서 __init__재정의시 부모의 초기화가 필요한지 확인 
3. 라이브러리 속성에 데이터를 넣을 시 요구하는 자료구조를 점검합니다. 


### Next: 




