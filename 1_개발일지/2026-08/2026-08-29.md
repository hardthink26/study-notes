# 개발일지 

## block1 
### 1. 구현 목적: make post model and route 

### 2.개발흐름 :
1. post model제작(id, textbody, 생성시간등등 포함)
	->지정된 유저의 post model이어야 함 
	-> User model과 연결되게 작성이 포인트 
2. PostForm만들기 
3. route 함수로 연결 

### 3.병목점/이유: 
1. User model에 db.relationship작성x 
	`Post.author_id → User.id`로 FK를 설정하고 `User.posts ↔ Post.author` relationship을 구성
2. posts = Post.query.order_by(Post.timestamp.desc()).all() 빠뜨림 
	=> 없으면 나중에 사용자 화면에 작성 포스트 전시x 
3. route함수에 현 유저의 정보 안가지고 옴(current_user._get_current_object())
4. Post model에서 timestamp column에서 index 속성x/유저에게 날짜 작성 맡김 
	=> index가 있다면 나중에 날짜 별 검색 용이
	=> 유저에게 날짜 적게 할 필요 없음, db작성 시 날짜 입력되게 설정->default이용 
		**ORM 객체 생성 순간이 아니라 INSERT/flush 시점에 default callable이 평가됨** 


## block2
### 1. 구현 목적: 

### 2.개발흐름 :

### 3.병목점/이유: 

## block3
### 1. 구현 목적: 

### 2.개발흐름 :

### 3.병목점/이유: 
---
### 배울 지점: 
1. form의 형태라고 해도 모두 유저가 작성해야하는것은 아님 
2. current_user 개념 체크 
3. db.relationship 개념 점검 


### Next: 




