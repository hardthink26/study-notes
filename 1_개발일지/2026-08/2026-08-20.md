# 개발일지 


## block1 
### 1. 구현 목적: 
유저들에게 role assignment + 유저들 퍼밋 여부 확인 

### 2.개발흐름 :
1. User class에 role assignment 함수 등록 
2. 등록된 유저의 메일이 관리자 메일과 같다면 관리자 권한 부여 아니면 전부 default=True인 role할당 
	-> #퍼밋함수 
--- 
1. User class에 퍼밋 확인 함수 등록 
	-> 퍼밋확인 로직은 role에서 짠 퍼밋 확인 함수를 사용한다. 
2. Anonymous user또한  퍼밋 판별 로직을 등록/ 모두 False를 반환하게 작성 
	-> 왜 필요? 나중에 일일이 로그인 여부 검사할 필요가 없도록 하기 위해서

### 3.병목점/이유: 
1. self.role is None인 상태에서는 self.role.name에 접근 불가 #DB상태 
	-> DB에서 Role객체를 조회해서 할당해야한다. 

## block2 
### 1. 개발목적 
권한 체크 로직 개발 
### 2. 개발 흐름 
1. permission_required(permission)함수 
	-> 필요한 권한을 받음 
	-> 보호할 함수를 받음 
	-> 실행시 current_user.can()검사 
	-> 실패시 403 에러 /성공 시 원래 함수 실행 
### 3. 병목지점 
1. functions.wraps 의 내부 동작 이해 부족 #deep-dive
2. closure/parameterized decorator추가 학습 필요 #deep-dive 

# block 3 
--- 
### 1. 개발목적 
refactoring: 기존 Permission class에서 값을 getattr이 아닌 숫자로 받아오게 전환 
### 2. 개발흐름 
1. insert_roles함수 정의시 Follow같은 문자가 아닌 Permission.Follow같은 숫자값으로 받는다
--- 
배울 지점 : 
- User -Role 관계는 Role객체를 조회해서 할당 
- Boolean expression 직접 return 
- 일반 권한 판정 can() 재사용 -> 중복 제거 
- AnonymousUser ->비로그인 특수 케이스를 동일 인터페이스 처리 
- Permission 전달 형식을 int값으로 통일 
- Decorator: permission -> 함수 ->실행 시 검사 구조 






